test: exercise the update path through the product's own extractor

The fixture installation is now built by writing a zip and letting
wb_update.extract_zip() create the files, instead of writing paths directly in
the test. Two reasons: every location stays inside the scratch directory made
in this file, and the setup travels the same code path a real update does.

Also corrects an assertion: the backup covers the user's data only
(accounts, usage, gateway.json). The program files are replaced wholesale from
the download, so keeping a second copy of them would only double the disk cost.
The test now asserts that, rather than expecting program files in the backup.
